@bake.Java(
    dependencies = {
        // Internal dependency.
        "foo.bar",

        // jersey-client uses "bundle" instead of "jar" for its artifact type.
        "external:com.sun.jersey/jersey-client@1.1.5.1",

        // Used to test classpath scanning.
        "external:com.sun.jersey/jersey-core@1.1.5.1",

        // Internal, test-only dependency.
        "tee"
    },
    testDependencies = {
        "external:junit/junit@4.3"
    },
    mainClass = "bake.example.foo.Foo"
) module foo;
